{
  "name": "exercicio-sass",
  "version": "1.0.0",
  "description": "Exercício SASS — conversão do CSS para SASS com variáveis e módulos",
  "scripts": {
    "sass": "sass --watch sass/main.scss:styles.css"
  },
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
